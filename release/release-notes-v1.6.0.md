NexaPlay v1.6.0 keeps every Player library current with the SauceBoyz catalog.

- Catalog sync now bypasses stale GitHub caching.
- An open game page refreshes immediately after a sync.
- The Player refreshes every five minutes and when it becomes active again.
- SteamGridDB artwork uses the Steam App ID first and keeps partial artwork results when one category is unavailable.
- PEAK and Forza Horizon 6 ship with the current metadata and artwork.
- PC compatibility results now clearly identify the check as partial: Windows, RAM, and storage are verified; CPU and GPU performance remain unranked.
- Physical GPU detection ignores the SudoMaker virtual display adapter.

Download `NexaPlay-Setup-v1.6.0.exe` and run it. Existing settings, installed games, favorites, and saved PC information are preserved.
